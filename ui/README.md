# UI

UI is the place to write generic UI components.

Let's start with the theme in [theme](theme.tsx)

Then we write components in [button](button.tsx)

Then we write a demo in [button-demo](button-demo.tsx)

In playground we combine the buttons.

## Playground

The playground should be simple, sidebar with all the components and the right content shows
whatever is choosen from the sidebar.

