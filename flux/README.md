# Flux

Flux is a state management library that is cross platform and works with SQLite.
For web, it uses OPFS and for rest its just sqlite.

Flux uses LegendState under the hood and handles sync

Flux is actually not the place to write app logic. Consider this folder as a library that you can use everywhere.
